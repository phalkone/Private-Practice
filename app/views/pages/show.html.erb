<% if @subpages %> 
  <div id="subcontent">
<% else %>
  <div id="pagecontent">
<% end %>
    <% if @page.html %>
      <%= raw @page.body %>
    <% else %>
      <%= raw BlueCloth.new(@page.body).to_html %>
    <% end %>
  </div>
<% if @subpages %> 
  <div id="submenu">
    <ul>
      <% for subpage in @subpages %>
        <% if subpage.nested and subpage == @page %>
          <li class="indent bold">
        <% elsif subpage.nested and subpage != @page %>
          <li class="indent">
        <% elsif subpage == @page %>
          <li class="bold">
        <% else %>
          <li>
        <% end %>
        <%= link_to subpage.title, page_path(subpage) %></li>
      <% end %>
    </ul>
  </div>
<% end %>
