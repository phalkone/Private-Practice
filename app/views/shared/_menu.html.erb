<ul>
  <% for menuitem in Page.where("nested = ?",false).order("sequence ASC") %>
    <% if @showpage and @showpage == menuitem %>
      <li class="active">
    <% elsif @subpages and @subpages.first == menuitem %>
      <li class="active">
    <% else %>
      <li>
    <% end %>
    <%= link_to menuitem.get_content(I18n.locale).title, page_path(menuitem) %></li>
  <% end %>
  <% if role?("doctor") %>
    <li class="<%= "active" if (controller.controller_name == "appointments") -%>">
      <%= link_to t("appointments.menutitle"), appointments_path %></li>
  <% end %>
  <% if role?("admin") %>
    <li class="<%= "active" if (controller.controller_name == "users") -%>">
      <%= link_to t("users.menutitle"), users_path %></li>
    <li class="<%= "active" if ((controller.controller_name == "pages") || 
                               (controller.controller_name == "images")) &&
                               (@showpage.nil?) -%>">
    <%= link_to t("pages.menutitle"), pages_path %></li>
  <% end %>
  <% if role?("patient") %>
    <li class="<%= "active" if (controller.controller_name == "bookings") -%>">
      <%= link_to t("bookings.menutitle"), bookings_path %></li>
  <% end %>
</ul>
